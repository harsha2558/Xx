name: Node.js CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x]

    env:
      SESSION_ID: 𝙰𝚂𝙸𝚃𝙷𝙰-𝙼𝙳=LvQwCbIJ#gf98o0D5L5048N9S2FkAduDfkdChOkk1SAcYwiO12n0
      GITHUB_AUTH_TOKEN: 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
      BOT_NUMBER: 94727786760

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}

    - name: Install dependencies
      run: npm install

    - name: Start application
      run: npm start
